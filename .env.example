PORT=3000
SESSION_SECRET=replace-with-a-long-random-string

# n8n webhook — handles Slack (#security + host DM) and Google Calendar
N8N_WEBHOOK_URL=https://n8n.anvil.ai/webhook/visitor-checkin

# Fallback direct Slack webhook (only used if N8N_WEBHOOK_URL is not set)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
